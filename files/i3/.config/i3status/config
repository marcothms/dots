general {
    output_format = "i3bar"
    colors = false
    markup = pango
    interval = 1
}

order += "read_file backlight"
order += "battery all"
order += "time"

time {
  format = " %a %-d.%b %H:%M"
}
